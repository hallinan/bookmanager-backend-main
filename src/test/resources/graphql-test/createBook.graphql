mutation createBook($title: String!, $authorId: Int!, $publishedDate: String!) {
    createBook(title: $title, authorId: $authorId, publishedDate: $publishedDate) {
        id
        title
        author {
            name
        }
        publishedDate
    }
}